import 'package:flutter/material.dart';

class AddressNo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return TextField(
      decoration: InputDecoration(labelText: 'Address No'),
    );
  }
}
